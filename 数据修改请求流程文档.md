# Vue-Admin 内容管理系统 - 数据修改请求流程文档

## 问答式请求流程说明

---

###  问题 1：当修改一条数据时，点击确认，发送了什么请求？

**答：** 发送了 **POST 请求**，携带 JSON 格式的文章数据。

---

### ❓ 问题 2：发送的数据是什么？

**答：** 发送的数据格式如下：

```json
{
  "id": 1,
  "title": "文章标题",
  "author": "作者名",
  "pageviews": 100,
  "status": "published",
  "content": "文章内容",
  "display_time": "2024-12-31 23:59:59"
}
```

**代码位置：** [`src/api/article.js` 第 15-16 行](file:///e:/前后端分离开发测试/vue-admin-template-master/vue-admin-template-master/src/api/article.js#L15-L16)

```javascript
export function updateArticle(id, data) {
  return request({ url: '/vue-admin-template/table/update', method: 'post', data: { id, ...data } })
}
```

---

### ❓ 问题 3：请求的 URL 是什么？

**答：** `/vue-admin-template/table/update`

---

### ❓ 问题 4：请求发给了哪个服务器？前端还是后端？

**答：** 请求首先发给 **前端开发服务器**（`http://localhost:9528`），然后通过代理转发到 **后端 Flask 服务器**（`http://localhost:5000`）。

**代码位置：** [`vue.config.js` 第 39-43 行](file:///e:/前后端分离开发测试/vue-admin-template-master/vue-admin-template-master/vue.config.js#L39-L43)

```javascript
proxy: {
  '/vue-admin-template': {
    target: 'http://localhost:5000',  // Flask 后端
    changeOrigin: true
  }
}
```

---

### ❓ 问题 5：有没有进行转发？

**答：** **有转发**。前端开发服务器会将请求代理转发到后端。

---

### ❓ 问题 6：转发规则是什么？

**答：** 所有以 `/vue-admin-template` 开头的请求都会被转发到 `http://localhost:5000`。

| 配置项 | 值 | 说明 |
|--------|-----|------|
| 匹配路径 | `/vue-admin-template` | 以此开头的请求都会被代理 |
| 目标地址 | `http://localhost:5000` | Flask 后端服务地址 |
| changeOrigin | `true` | 修改请求头中的 Origin 字段 |

**代码位置：** [`vue.config.js` 第 39-43 行](file:///e:/前后端分离开发测试/vue-admin-template-master/vue-admin-template-master/vue.config.js#L39-L43)

---

### ❓ 问题 7：转发规则在哪个文件设置？

**答：** 在 [`vue.config.js`](file:///e:/前后端分离开发测试/vue-admin-template-master/vue-admin-template-master/vue.config.js#L39-L43) 文件的 `devServer.proxy` 配置中设置。

---

### ❓ 问题 8：后端哪个视图函数处理了该请求？

**答：** [`app.py`](file:///e:/前后端分离开发测试/vue-admin-template-master/vue-admin-template-master/app.py#L237) 中的 `update_article` 函数。

**代码位置：** [`app.py` 第 237 行](file:///e:/前后端分离开发测试/vue-admin-template-master/vue-admin-template-master/app.py#L237)

```python
@app.route('/vue-admin-template/table/update', methods=['POST'])
@token_required
def update_article(payload):
    try:
        data = request.get_json()
        article_id = data.get('id')
        # ... 处理更新逻辑
```

---

### ❓ 问题 9：数据库的表是不是基于 SQLAlchemy 实现的？

**答：** **不是**。当前项目使用的是 **PyMySQL** 原生 SQL 查询，不是 SQLAlchemy ORM。

**代码位置：** [`models.py` 第 47-60 行](file:///e:/前后端分离开发测试/vue-admin-template-master/vue-admin-template-master/models.py#L47-L60)

```python
def get_db_connection():
    connection = pymysql.connect(
        host=Config.MYSQL_HOST,
        port=Config.MYSQL_PORT,
        user=Config.MYSQL_USER,
        password=Config.MYSQL_PASSWORD,
        database=Config.MYSQL_DB,
        charset='utf8mb4',
        cursorclass=pymysql.cursors.DictCursor
    )
    return connection
```

---

### ❓ 问题 10：如果查询语句包含原始 SQL，如何改成基于 ORM 的方式？

**答：** 可以改用 SQLAlchemy ORM 方式，示例如下：

**当前方式（PyMySQL 原始 SQL）：**
```python
cursor.execute("UPDATE articles SET title=%s WHERE id=%s", (title, article_id))
```

**改为 SQLAlchemy ORM 方式：**

```python
from sqlalchemy import create_engine, Column, Integer, String, Text, DateTime
from sqlalchemy.orm import declarative_base, sessionmaker

Base = declarative_base()

class Article(Base):
    __tablename__ = 'articles'
    id = Column(Integer, primary_key=True, autoincrement=True)
    title = Column(String(255), nullable=False)
    author = Column(String(50), nullable=False)
    content = Column(Text)
    pageviews = Column(Integer, default=0)
    status = Column(String(20), default='draft')
    display_time = Column(DateTime)

# 初始化数据库连接
engine = create_engine('mysql+pymysql://user:pass@localhost:3306/db')
Session = sessionmaker(bind=engine)

# 更新操作
def update_article_orm(article_id, **kwargs):
    session = Session()
    try:
        article = session.query(Article).filter_by(id=article_id).first()
        if article:
            for key, value in kwargs.items():
                if value is not None and hasattr(article, key):
                    setattr(article, key, value)
            session.commit()
            return True
        return False
    finally:
        session.close()
```

---

### ❓ 问题 11：视图函数返回了什么格式的数据给前端？

**答：** 返回 **JSON 格式** 数据。

**代码位置：** [`app.py` 第 295-303 行](file:///e:/前后端分离开发测试/vue-admin-template-master/vue-admin-template-master/app.py#L295-L303)

```python
# 成功响应
return jsonify({'code': 20000, 'data': 'success'})

# 失败响应
return jsonify({'code': 50005, 'message': '更新操作未生效'}), 500

# 异常响应
return jsonify({'code': 50000, 'message': f'更新文章失败: {str(e)}'}), 500
```

---

### ❓ 问题 12：前端哪段代码用来接收后端发来的数据？

**答：** 通过 Axios 响应拦截器接收。

**代码位置：** [`src/utils/request.js` 第 33-50 行](file:///e:/前后端分离开发测试/vue-admin-template-master/vue-admin-template-master/src/utils/request.js#L33-L50)

```javascript
service.interceptors.response.use(
  response => {
    const res = response.data  // 这里接收后端返回的 JSON 数据
    
    if (res.code !== 20000) {
      Message({
        message: res.message || 'Error',
        type: 'error',
        duration: 5 * 1000
      })
      return Promise.reject(new Error(res.message || 'Error'))
    }
    return res
  }
)
```

---

## 完整请求链路总结

| 步骤 | 说明 | 文件 | 行号 |
|------|------|------|------|
| 1 | 用户点击确认按钮 | `src/views/article/list.vue` | - |
| 2 | 调用 updateArticle API | `src/api/article.js` | 15-16 |
| 3 | Axios 发送 POST 请求 | `src/utils/request.js` | 7-11 |
| 4 | 请求拦截器添加 Token | `src/utils/request.js` | 14-27 |
| 5 | 开发代理转发请求 | `vue.config.js` | 39-43 |
| 6 | Flask 路由匹配 | `app.py` | 237 |
| 7 | Token 验证装饰器 | `app.py` | 53-72 |
| 8 | 接收 JSON 数据 | `app.py` | 239-240 |
| 9 | 验证文章 ID | `app.py` | 242-249 |
| 10 | 执行数据库更新 | `models.py` | Article.update() |
| 11 | 返回 JSON 响应 | `app.py` | 295-303 |
| 12 | 响应拦截器处理 | `src/utils/request.js` | 33-50 |
| 13 | 前端更新 UI | `src/views/article/list.vue` | - |
